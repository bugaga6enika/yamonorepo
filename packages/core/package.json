{
  "name": "@repo/core",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "test": "vitest run",
    "lint": "echo 'Add lint script here'"
  },
  "devDependencies": {
    "@repo/vitest-config": "*",
    "vitest": "^4.1.2"
  }
}
